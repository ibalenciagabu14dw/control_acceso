$(document).ready(function() {
	/*$('#loginForm').submit(function(event) {
		event.preventDefault();
		var user = $('#user').val();
		var pass = $('#pass').val();
		var admin;
		if ($('#administrador').is(':checked')) {
			admin = 1;
		}else{
			admin = 0;
		}
		data = {'user':user,'pass':pass,'admin':admin};
		$.ajax({
			url: '/login',
			type: 'post',
			data: data,
		})
		.done(function() {
			console.log("success");
		})
		.fail(function() {
			console.log("error");
		})//ajax
		
	});//submit*/
});//ready